export const SPRING_SNAPPY = { type: 'spring' as const, stiffness: 400, damping: 25 };

export const STAGGER_ITEM = { delayMultiplier: 0.04, duration: 0.25, ease: 'easeOut' as const };

export const STAGGER_METRIC = { delayMultiplier: 0.08, duration: 0.25, ease: 'easeOut' as const };
